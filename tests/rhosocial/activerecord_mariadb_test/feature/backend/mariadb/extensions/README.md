# extensions tests

MariaDB vendor extension features: FULLTEXT index protocol and MATCH usage and the SET type (protocol-level support plus real-database integration).

## Key files

- `test_fulltext_index.py` — FULLTEXT protocol and MATCH expressions
- `test_set_type.py` — SET type protocol
- `test_set_type_backend.py` — SET type integration
