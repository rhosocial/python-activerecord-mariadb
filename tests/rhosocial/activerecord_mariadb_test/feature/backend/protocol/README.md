# protocol tests

MariaDBBackend dialect-level protocol conformance: statement support surface, non-overlap guarantees and mixin coverage.

## Key files

- `test_protocol_conformance.py` — dialect protocol conformance and non-overlap
