Split the monolithic `protocols.py` into a modular `protocols/` package (one file per feature, mirroring `mixins/`); a backward-compatibility shim re-exports all protocols so existing imports are unaffected. Rewrote `types.py` to re-export the `DataType` subclasses from `expression.types`, resolving a shadowing conflict where pre-#108 helper classes masked the correct `DataType` implementations.
