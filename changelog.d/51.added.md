Scenario filtering now also honors the generic `TESTSUITE_ACTIVE_SCENARIOS` env var (falling back from `MARIADB_ACTIVE_SCENARIOS`) so `--scenarios` works when running tests from either the testsuite directory or this repository.