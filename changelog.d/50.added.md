Added `--async` support to the `named-migration` CLI subcommand, enabling asynchronous migration execution via `AsyncMariaDBBackend` and `AsyncMigrationRunner` (requires core `AsyncNamedMigration`). Added `examples/schema_diff/` (add_table, add_column, index_change, serialization_roundtrip) and `examples/named_migrations/` including async migration demos.
